const fruits = ["Banana", "Orange", "Apple", "Mango"];

// length
let size = fruits.length;

// toString()
let fruitsToString = fruits.toString();

// pop()
let popedFruit = fruits.pop();

//push()
let pushedFruit = fruits.push("Mojitos")

// shift()
let shiftedFruit = fruits.shift()

// unshift()
let unshiftedFruit = fruits.unshift();

// delete()
let deletedFruit = fruits.delete;

// concant()

const anotherFruits = ["Mojitos", "Melon", "Macadamia", "Pinapple"]

const allFruits = fruits.concat(anotherFruits);

console.log(allFruits)